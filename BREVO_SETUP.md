# 📧 Настройка Brevo (Sendinblue) для PAUL Azerbaijan

## ✅ Преимущества Brevo

- ✅ **300 писем/день бесплатно** (9,000/месяц)
- ✅ **Нет верификации телефона**
- ✅ **Простая настройка** - 5 минут
- ✅ **Отличная доставляемость** 95%+
- ✅ **Русский интерфейс**
- ✅ **SMS и WhatsApp** (опционально)

---

## Шаг 1: Регистрация в Brevo

1. Перейдите на **https://www.brevo.com/**
2. Нажмите **"Sign up free"**
3. Заполните форму:
   - Email: ваш рабочий email
   - Пароль: создайте надежный пароль
   - Имя компании: `PAUL Azerbaijan`
4. Нажмите **"Create my account"**
5. Подтвердите email (проверьте входящие)

**Важно**: Телефон НЕ требуется! 🎉

---

## Шаг 2: Получение API ключа

После входа в аккаунт:

1. Нажмите на **⚙️ Settings** (в правом верхнем углу)
2. В левом меню выберите **SMTP & API**
3. Перейдите на вкладку **API Keys**
4. Нажмите **"Generate a new API key"**
5. Введите имя ключа: `PAUL Azerbaijan Production`
6. Нажмите **"Generate"**
7. **СКОПИРУЙТЕ КЛЮЧ** (начинается с `xkeysib-...`)

⚠️ **Важно**: Ключ показывается только один раз! Сохраните его.

---

## Шаг 3: Настройка отправителя (Sender)

1. В Brevo перейдите в **Senders & IP**
2. Нажмите **"Add a sender"**
3. Заполните:
   - **Email**: `noreply@paul-azerbaijan.com`
   - **Name**: `PAUL Azerbaijan`
4. Нажмите **"Save"**
5. Подтвердите email (письмо придет на `noreply@paul-azerbaijan.com`)

**Примечание**: Для подтверждения нужен доступ к этому email. Если нет доступа, используйте свой email временно для тестирования.

---

## Шаг 4: Обновление .env на сервере

Подключитесь к серверу и обновите конфигурацию:

```bash
ssh root@46.62.152.225
nano /var/www/paul/backend/.env
```

Найдите секцию **MAIL** и замените на:

```env
# ============================================
# EMAIL (Brevo)
# ============================================
MAIL_MAILER=brevo
BREVO_API_KEY=xkeysib-ваш_api_ключ_из_brevo

# Email Settings
MAIL_FROM_ADDRESS="noreply@paul-azerbaijan.com"
MAIL_FROM_NAME="PAUL Azerbaijan"
```

**Замените**:
- `xkeysib-ваш_api_ключ_из_brevo` → на ваш API ключ из Brevo

Сохраните файл:
- `Ctrl+O` → Enter → `Ctrl+X`

---

## Шаг 5: Перезапуск контейнеров

```bash
cd /var/www/paul/docker
docker compose restart backend queue
```

Подождите 10 секунд, пока контейнеры перезапустятся.

---

## Шаг 6: Тестирование

### Вариант 1: Через тестовый скрипт

```bash
docker exec paul_backend php /var/www/html/test-email.php your-email@example.com
```

Замените `your-email@example.com` на ваш реальный email.

### Вариант 2: Через tinker

```bash
docker exec -it paul_backend php artisan tinker
```

В tinker выполните:

```php
Mail::raw('Тестовое письмо от PAUL Azerbaijan через Brevo! 🎉', function($message) {
    $message->to('your-email@example.com')
            ->subject('✅ Тест Email - PAUL Azerbaijan');
});
```

Нажмите `Ctrl+D` для выхода.

---

## Шаг 7: Проверка результатов

### Проверьте email
1. Откройте входящие на `your-email@example.com`
2. Если письма нет, проверьте **папку Спам**

### Проверьте логи Brevo
1. Перейдите в Brevo → **Statistics** → **Email**
2. Вы увидите статистику отправленных писем

### Проверьте логи Laravel
```bash
docker exec paul_backend tail -f storage/logs/laravel.log
```

Ищите строки с `[mail]` или ошибки.

---

## 📊 Мониторинг

### В Brevo Dashboard:

1. **Statistics** → **Email**
   - Отправлено
   - Доставлено
   - Открыто
   - Клики

2. **Logs** → **Email logs**
   - Детальная информация по каждому письму
   - Статус доставки
   - Ошибки

---

## 🎯 Лимиты бесплатного плана

| Параметр | Лимит |
|----------|-------|
| Писем в день | 300 |
| Писем в месяц | ~9,000 |
| Контактов | Безлимит |
| Отправителей | 1 |
| Хранение логов | 7 дней |

**Для увеличения лимитов**: Upgrade до платного плана (от $25/месяц за 20,000 писем)

---

## 🔧 Troubleshooting

### Ошибка: "Sender not authorized"

**Причина**: Email отправителя не подтвержден в Brevo

**Решение**:
1. Brevo → **Senders & IP**
2. Проверьте статус отправителя
3. Подтвердите email, если требуется

### Ошибка: "Invalid API key"

**Причина**: Неправильный API ключ в .env

**Решение**:
1. Проверьте API ключ в Brevo
2. Убедитесь, что в .env нет лишних пробелов
3. Перезапустите контейнеры

### Письма не приходят

**Проверьте**:
1. Логи Brevo (Email logs)
2. Папку спам
3. Логи Laravel: `docker logs paul_backend | grep -i mail`
4. Email отправителя подтвержден

### Письма попадают в спам

**Решение**:
1. Добавьте SPF запись (см. ниже)
2. Прогрейте домен (отправляйте письма постепенно)
3. Используйте подтвержденный домен

---

## 🚀 Дополнительно: Настройка собственного домена (опционально)

Для лучшей доставляемости настройте отправку с вашего домена:

### 1. В Brevo

1. **Settings** → **Senders & IP** → **Domains**
2. Нажмите **"Add a domain"**
3. Введите: `paul-azerbaijan.com`
4. Brevo покажет DNS записи

### 2. В Namecheap (Advanced DNS)

Добавьте записи из Brevo:

| Type | Host | Value | TTL |
|------|------|-------|-----|
| TXT | @ | `v=spf1 include:spf.brevo.com ~all` | Automatic |
| TXT | mail._domainkey | `k=rsa; p=MIG...` (из Brevo) | Automatic |

### 3. Проверка

В Brevo нажмите **"Verify domain"**. Подождите 5-30 минут.

---

## ✨ Готово!

Теперь ваш сайт отправляет email через Brevo!

### Что работает:

✅ Уведомления о новых заявках  
✅ Подтверждения заказов  
✅ Уведомления об оплате  
✅ Уведомления персоналу  
✅ Изменения статусов  

### Следующие шаги:

1. Протестируйте все типы уведомлений
2. Настройте email шаблоны в Brevo (опционально)
3. Добавьте домен для лучшей доставляемости
4. Мониторьте статистику отправок

---

## 📞 Поддержка

- **Документация Brevo**: https://developers.brevo.com/
- **Поддержка**: https://help.brevo.com/
- **Статус сервиса**: https://status.brevo.com/

