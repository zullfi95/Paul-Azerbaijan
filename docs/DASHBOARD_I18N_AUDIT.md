# 🔍 Аудит интернационализации дашборда

**Дата:** 17 ноября 2024  
**Статус:** ❌ Несинхронизация языков

---

## 📊 Текущее состояние

### ✅ Страницы с переводами:
1. **dashboard/orders/page.tsx** - использует `useTranslations()` ✅
2. **dashboard/calendar/page.tsx** - использует `useTranslations()` ✅

### ❌ Страницы БЕЗ переводов (хардкод на русском):
1. **dashboard/page.tsx** - главная страница дашборда
2. **dashboard/applications/page.tsx** - страница заявок
3. **dashboard/users/page.tsx** - страница пользователей
4. **dashboard/positions/page.tsx** - страница позиций меню
5. **dashboard/reports/page.tsx** - страница отчетов
6. **dashboard/beo/page.tsx** - страница BEO

---

## 🔧 Что нужно исправить

### 1. dashboard/page.tsx
**Непереведенные строки:**
- "Заявки" → `t('dashboard.applications')`
- "Новых: X | В работе: Y" → `t('dashboard.newApplications')` + `t('dashboard.inProcessing')`
- "Заказы" → `t('dashboard.orders')`
- "В работе: X | Завершено: Y" → `t('orders.status.processing')` + `t('dashboard.completedOrders')`
- "Время" → `t('common.time')`
- "Просмотр заявок" → `t('applications.title')`
- "Создать заказ" → `t('orders.createOrder')`
- "Пользователи" → `t('dashboard.users')`
- "Календарь заказов" → `t('calendar.title')`
- "Последние заявки" → `t('applications.title')`
- "Все →" → `t('common.all')`
- "Заявитель", "Контакты", "Мероприятие", "Статус", "Дата", "Действия" → переводы для таблицы
- "Последние заказы" → `t('orders.title')`
- И много других строк...

### 2. dashboard/applications/page.tsx
**Непереведенные строки:**
- "Новые", "В обработке", "Одобренные", "Отклоненные" → `t('applications.newApplications')`, etc.
- "Всего заявок" → `t('applications.totalApplications')`
- "Экспорт", "Удалить", "Изменить статус" → `t('applications.export')`, etc.
- Все заголовки таблиц и кнопки

### 3. dashboard/users/page.tsx
**Непереведенные строки:**
- "Всего пользователей" → `t('users.totalUsers')`
- "Активные", "Неактивные" → `t('users.activeUsers')`, `t('users.inactiveUsers')`
- "Добавить пользователя" → `t('users.addUser')`
- Все поля формы и таблицы

### 4. dashboard/positions/page.tsx
**Непереведенные строки:**
- "Позиции меню" → `t('positions.title')`
- "Добавить новую позицию" → `t('positions.addNew')`
- "Без категории" → нужно добавить в переводы
- Все сообщения об ошибках и успехе

### 5. dashboard/reports/page.tsx
**Непереведенные строки:**
- "Отчеты" → `t('reports.title')`
- "Диапазон дат" → `t('reports.dateRange')`
- "Создать отчет" → `t('reports.generate')`
- Все метрики и таблицы

### 6. dashboard/beo/page.tsx
**Непереведенные строки:**
- Все заголовки и кнопки

---

## 📝 План действий

1. ✅ Добавить `useTranslations()` во все страницы дашборда
2. ✅ Заменить все хардкод строки на `t()` вызовы
3. ✅ Проверить, что все ключи переводов существуют в `ru.json` и `az.json`
4. ✅ Добавить недостающие ключи переводов
5. ✅ Протестировать переключение языков на всех страницах

---

## 🎯 Приоритет

**КРИТИЧНО** - весь дашборд должен быть на двух языках (азербайджанский и русский) согласно требованиям пользователя.

