# 🔐 Настройка переменных окружения на сервере

## Важно!

После обновления `docker-compose.yml` нужно создать файл `docker.env` на сервере с реальными API ключами.

## Шаги на сервере:

### 1. Создать файл docker.env

```bash
cd /var/www/paul/docker
cp docker.env.example docker.env
```

### 2. Заполнить реальными значениями

Отредактировать `docker.env` и заполнить реальными ключами:

```bash
nano docker.env
```

**Обязательно заполнить:**
- `MAIL_USERNAME` - ваш SMTP email
- `MAIL_PASSWORD` - ваш SMTP пароль
- `IIKO_API_LOGIN` - логин iiko
- `IIKO_API_PASSWORD` - пароль iiko
- `ALGORITMA_API_KEY` - ключ Algoritma
- `ALGORITMA_API_SECRET` - секрет Algoritma

### 3. Проверить права доступа

```bash
chmod 600 docker.env  # Только владелец может читать/писать
```

### 4. Перезапустить контейнеры

```bash
docker-compose down
docker-compose up -d
```

---

## ⚠️ ВАЖНО: Ключи НЕ будут потеряны!

Если на сервере уже есть работающие контейнеры с ключами в docker-compose.yml, просто:

1. Скопируйте значения из старого docker-compose.yml в новый docker.env файл
2. Или создайте docker.env с теми же значениями, что были в docker-compose.yml

**Ключи останутся на сервере, их не нужно вводить заново!**

