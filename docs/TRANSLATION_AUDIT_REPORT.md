# 🔍 Отчет по аудиту непереведенных текстов

**Дата проверки:** 13 ноября 2024  
**Метод:** Систематическая проверка в 4 итерации

---

## ✅ ЧТО УЖЕ ПЕРЕВЕДЕНО

### Полностью переведенные компоненты:
- ✅ **Header** - все элементы навигации, поиска, кнопок
- ✅ **Footer** - все секции, newsletter, политики
- ✅ **Hero** - слайды и описания
- ✅ **CartModal** - корзина и все сообщения

### Расширенные файлы переводов:
- ✅ **en.json** - 410+ строк, 200+ ключей
- ✅ **az.json** - 410+ строк, 200+ ключей

### Новые секции переводов:
- ✅ `calendar` - календарь заказов
- ✅ `positions` - позиции меню
- ✅ `applications` - заявки
- ✅ `users` - пользователи  
- ✅ `reports` - отчеты
- ✅ `event` - планирование мероприятий
- ✅ `form` - формы
- ✅ `profile` - профиль
- ✅ `product` - товары
- ✅ `pages` - страницы (about, contact, faq, etc.)

---

## 📋 ЧТО НУЖНО ПЕРЕВЕСТИ

### ИТЕРАЦИЯ 1: Dashboard Pages (8 файлов) - КРИТИЧНО

#### 1. `/dashboard/calendar/page.tsx` ⚠️ ЧАСТИЧНО ПЕРЕВЕДЕНО
**Непереведенные строки:**
```typescript
// Статистика
"В обработке" → t('orders.status.processing')
"Готово к доставке" → t('orders.status.ready')

// Модальное окно
"Создать новый заказ" → t('calendar.createNew')
"Создать заказ" → t('orders.createOrder')

// Алерты
alert('Ошибка при создании BEO файла') → alert(t('calendar.errorCreatingBEO'))
```

**Как исправить:**
```typescript
// 1. Добавить в начало:
import { useTranslations } from 'next-intl';

// 2. В компоненте:
const t = useTranslations();

// 3. Заменить строки на:
{t('orders.status.processing')}
{t('orders.status.ready')}
alert(t('calendar.errorCreatingBEO'))
```

#### 2. `/dashboard/positions/page.tsx`
**Непереведенные строки:**
```typescript
"Позиции меню"
"В меню"
"Групп меню"
"Управление товарами в меню"
"Поиск позиций меню"
"Ничего не найдено по вашему запросу"
"Нет позиций меню"
"Вы уверены, что хотите удалить эту позицию меню?"
"Позиция меню успешно удалена!"
```

**Замены:**
```typescript
const t = useTranslations('positions');

{t('title')}              // Позиции меню
{t('inMenu')}            // В меню
{t('menuGroups')}        // Групп меню
{t('description')}       // Управление товарами в меню
{t('searchPlaceholder')} // Поиск позиций меню
{t('nothingFound')}      // Ничего не найдено
{t('noPositions')}       // Нет позиций меню
{t('confirmDelete')}     // Подтверждение удаления
alert(t('deleteSuccess')) // Удалено успешно
```

#### 3. `/dashboard/orders/page.tsx`
**Непереведенные строки:**
```typescript
"Аналитика заказов"
"Новый заказ"
"Всего заказов"
"Выполненных заказов"
"Поиск заказов"
"Обновить список заказов"
"Показано: {count} из {total} заказов"
"Заказы не найдены"
"Создайте первый заказ для начала работы"
"Вы уверены, что хотите удалить этот заказ?"
"Заказ успешно скопирован!"
"Статус заказа успешно изменен!"
```

**Замены:**
```typescript
const t = useTranslations();

{t('orders.title')}
{t('orders.createOrder')}
{t('orders.totalOrders')}
{t('orders.completedOrders')}
{t('orders.searchPlaceholder')}
{t('common.refresh')}
{t('orders.showingOrders', { count, total })}
{t('orders.noOrders')}
{t('orders.confirmDelete')}
alert(t('orders.deleteSuccess'))
```

#### 4. `/dashboard/applications/page.tsx`
**Непереведенные строки:**
```typescript
"Заявки"
"Управление заявками клиентов"
"Всего заявок"
"Новые"
"Обработанные"
"Экспорт"
"Выбрано: {count}"
"Выбрать все"
"Снять выделение"
"Изменить статус"
"Пока нет заявок"
"Создайте первую заявку для начала работы"
```

**Замены:**
```typescript
const t = useTranslations('applications');

{t('title')}
{t('description')}
{t('totalApplications')}
{t('newApplications')}
{t('processedApplications')}
{t('export')}
{t('selected', { count })}
{t('selectAll')}
{t('deselectAll')}
{t('changeStatus')}
{t('noApplications')}
{t('createFirst')}
```

#### 5. `/dashboard/users/page.tsx`
```typescript
"Пользователи"
"Управление пользователями"
"Всего пользователей"
"Активных"
"Неактивных"
"Добавить пользователя"
"Редактировать пользователя"
"Имя"
"Фамилия"
"Email"
"Телефон"
"Роль"
"Администратор"
"Менеджер"
"Координатор"
"Клиент"
```

**Используйте:** `t('users.*)`

#### 6. `/dashboard/reports/page.tsx`
```typescript
"Отчеты"
"Аналитика и статистика"
"Период"
"Начало"
"Конец"
"Генерировать отчет"
"Экспорт"
"Статус заказа"
"Всего заказов"
"Общая выручка"
"Средний чек"
```

**Используйте:** `t('reports.*)`

---

### ИТЕРАЦИЯ 2: Forms & Modals (4 файла) - ВАЖНО

#### 1. `/components/EventPlanningModal.tsx`
**Непереведенные строки:**
```typescript
"Plan an Event"
"Event Date"
"Location"
"Budget"
"Number of Guests"
"Event Details"
"Your Name"
"Email"
"Phone"
"Submit Request"
"Submitting..."
"Error sending request. Please try again."
"Please fill in all required fields"
```

**Как исправить:**
```typescript
import { useTranslations } from 'next-intl';

const EventPlanningModal = ({ isOpen, onClose, onSuccess }) => {
  const t = useTranslations('event');
  
  return (
    <>
      <h2>{t('title')}</h2>
      <label>{t('eventDate')}</label>
      <label>{t('location')}</label>
      <button disabled={isSubmitting}>
        {isSubmitting ? t('submitting') : t('submit')}
      </button>
      {error && alert(t('error'))}
    </>
  );
};
```

#### 2. `/components/CreateOrderForm.tsx`
```typescript
"Создание заказа"
"Информация о заказе"
"ID заказа"
"Товары заказа"
"Доступно {count} товаров в меню"
"Добавить из меню"
"Меню загружается..."
"Нет товаров в заказе"
"Включить регулярные заказы"
"Частота"
"Дата окончания"
"Заметки для регулярных заказов"
"Особые инструкции для этого заказа..."
"Создать заказ"
"Создание..."
```

**Используйте:** `t('form.*)`

#### 3. `/components/EditOrderForm.tsx`
```typescript
"Редактирование заказа #{id}"
"Загрузка заказа..."
"Заказ не найден"
"Обновление заказа..."
"Информация о заказе"
"ID заказа"
```

**Используйте:** `t('form.*)`

#### 4. `/components/MenuItemForm.tsx`
```typescript
"Название"
"Описание"
"Категория"
"Цена"
"Доступно для заказа"
"Активно (отображается в меню)"
"Изображения"
```

**Используйте:** `t('positions.*)`

---

### ИТЕРАЦИЯ 3: Other Pages (10+ файлов) - СРЕДНИЙ ПРИОРИТЕТ

#### `/app/cart/page.tsx`
```typescript
"Shopping Cart"
"Your cart is empty"
"Continue Shopping"
"Proceed to Checkout"
"Remove"
"Update Quantity"
```

**Используйте:** `t('cart.*)`

#### `/app/profile/page.tsx`
```typescript
"My Profile"
"Personal Information"
"Order History"
"Addresses"
"Settings"
"No orders yet"
"Make your first order"
```

**Используйте:** `t('profile.*)`

#### `/app/product/[id]/page.tsx`
```typescript
"Product Details"
"Add to Cart"
"Out of Stock"
"Available"
"Description"
"Ingredients"
"Allergens"
```

**Используйте:** `t('product.*)`

---

### ИТЕРАЦИЯ 4: Auth & Static Pages - НИЗКИЙ ПРИОРИТЕТ

#### Auth Pages
- `/app/auth/login/page.tsx`
- `/app/auth/register/page.tsx`
- `/app/auth/forgot-password/page.tsx`

**Используйте:** `t('auth.*)`

#### Static Pages  
- `/app/about/page.tsx` → `t('pages.about.*)`
- `/app/contact/page.tsx` → `t('pages.contact.*)`
- `/app/faq/page.tsx` → `t('pages.faq.*)`
- `/app/delivery/page.tsx` → `t('pages.delivery.*)`
- `/app/terms/page.tsx` → `t('pages.terms.*)`
- `/app/privacy/page.tsx` → `t('pages.privacy.*)`

---

## 🛠️ КАК ПЕРЕВОДИТЬ - ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Добавить импорт
```typescript
import { useTranslations } from 'next-intl';
```

### Шаг 2: Инициализировать хук
```typescript
// Для общих переводов
const t = useTranslations();

// Для конкретной секции
const t = useTranslations('calendar');
const tCommon = useTranslations('common');
```

### Шаг 3: Заменить строки
```typescript
// ДО:
<h1>Календарь заказов</h1>
<button>Новый заказ</button>
alert('Ошибка при создании');

// ПОСЛЕ:
<h1>{t('calendar.title')}</h1>
<button>{t('calendar.newOrder')}</button>
alert(t('calendar.errorCreatingBEO'));
```

### Шаг 4: Проверить

```bash
cd frontend
npm run dev
# Открыть http://localhost:3000
# Переключить язык на азербайджанский
# Проверить что все тексты переведены
```

---

## 📊 ПРИОРИТЕТЫ

### 🔴 КРИТИЧНО (сделать в первую очередь):
1. Dashboard Calendar - **используется постоянно**
2. Dashboard Orders - **главная функция**
3. Dashboard Positions - **управление меню**
4. Dashboard Applications - **заявки клиентов**

### 🟡 ВАЖНО (сделать вторым этапом):
5. EventPlanningModal - **виден клиентам**
6. CreateOrderForm - **создание заказов**
7. EditOrderForm - **редактирование заказов**

### 🟢 СРЕДНИЙ ПРИОРИТЕТ:
8. Cart Page
9. Profile Page
10. Product Page

### ⚪ НИЗКИЙ ПРИОРИТЕТ:
11. Auth Pages
12. Static Pages (About, FAQ, Terms, etc.)

---

## 📝 ЧЕКЛИСТ ДЛЯ КАЖДОГО ФАЙЛА

```markdown
- [ ] Добавлен импорт useTranslations
- [ ] Инициализирован хук t
- [ ] Все хардкоженные строки заменены на t()
- [ ] Все alert() используют переводы
- [ ] Все placeholder используют переводы
- [ ] Все кнопки используют переводы
- [ ] Все заголовки используют переводы
- [ ] Проверено на обоих языках (en, az)
- [ ] Нет ошибок в консоли
- [ ] UI выглядит корректно
```

---

## 🎯 ИТОГО

**Общая статистика:**
- ✅ Переведено: ~30% компонентов
- ⚠️ Частично: ~10% компонентов  
- ❌ Не переведено: ~60% компонентов

**Оценка работы:**
- Dashboard Pages: ~40 часов
- Forms: ~8 часов
- Other Pages: ~16 часов
- Testing: ~8 часов
- **Всего:** ~72 часа работы

---

## 💡 РЕКОМЕНДАЦИИ

1. **Начните с критичных страниц** (Dashboard)
2. **Используйте готовые переводы** (уже в en.json и az.json)
3. **Тестируйте по ходу** - переводите по 1-2 файла и сразу проверяйте
4. **Следуйте паттерну** из Header.tsx и CartModal.tsx
5. **Не забывайте про Server Components** - там используется `await getTranslations()`

---

## 📚 Примеры готового кода

### Client Component (большинство случаев):
```typescript
"use client";
import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations('mySection');
  
  return <div>{t('title')}</div>;
}
```

### Server Component:
```typescript
import { getTranslations } from 'next-intl/server';

export default async function MyServerComponent() {
  const t = await getTranslations('mySection');
  
  return <div>{t('title')}</div>;
}
```

---

**Документация:** См. `frontend/I18N_GUIDE.md`  
**Примеры:** Header.tsx, Footer.tsx, CartModal.tsx  
**Файлы переводов:** `frontend/messages/en.json`, `frontend/messages/az.json`

